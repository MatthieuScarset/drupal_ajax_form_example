# DEFINE DEV SPECIFIC VARIABLES AND JBS



stages:          # List of stages for jobs, and their order of execution
#  - prepare
#  - build_drupal
  - build_image
#  - push


Build Image:
  image: docker:latest
  services:
    - docker:dind
  stage: build_image
  script:
    - docker info
  tags:
    - image_build
