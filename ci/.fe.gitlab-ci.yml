# DEFINE DEV SPECIFIC VARIABLES AND JBS

image: docker:latest
services:
  - docker:dind

stages:          # List of stages for jobs, and their order of execution
#  - prepare
#  - build_drupal
  - build_image
#  - push


Build Image:
  stage: build_image
  script:
    - docker info
