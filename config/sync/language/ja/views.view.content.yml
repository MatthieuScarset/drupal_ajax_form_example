display:
  default:
    display_title: Default
    display_options:
      fields:
        type:
          label: 'Content type'
        operations:
          label: Operations
      exposed_form:
        options:
          submit_button: Filter
      filters:
        type:
          expose:
            label: 'Content type'
        langcode:
          expose:
            label: Language
