display:
  default:
    display_options:
      exposed_form:
        options:
          submit_button: Applicar
      fields:
        type:
          label: Provider
      filters:
        type:
          expose:
            label: Provider
