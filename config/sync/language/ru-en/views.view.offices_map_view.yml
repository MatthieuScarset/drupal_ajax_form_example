display:
  default:
    display_options:
      exposed_form:
        options:
          exposed_sorts_label: 'Sort by:'
  offices_map_block:
    display_options:
      exposed_form:
        options:
          exposed_sorts_label: 'Sort by:'
