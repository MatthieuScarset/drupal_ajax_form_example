display:
  default:
    display_options:
      exposed_form:
        options:
          submit_button: Найти
          exposed_sorts_label: Фильтр
      filters:
        redirect_redirect__uri:
          expose:
            label: Кому
