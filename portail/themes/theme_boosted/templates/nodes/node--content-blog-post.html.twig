<div id="content-blog-post">
    <div class="col-lg-12 col-md-12 col-sm-1 entete">
        {% for content in zones.entete %}
            {{ content }}
        {% endfor %}
    </div>
    <div class="col-lg-12 col-md-12 col-sm-1 haut">
        <div class="col-lg-6 col-md-6 col-sm-1 haut_gauche">
            {% for content in zones.hautgauche %}
                {{ content }}
            {% endfor %}
        </div>
        <div class="col-lg-6 col-md-6 col-sm-1 haut_droit">
            {% for content in zones.hautdroit %}
                {{ content }}
            {% endfor %}
        </div>
    </div>
    <div class="col-lg-12 col-md-12 col-sm-1 milieu">
        {% for content in zones.milieu %}
            {{ content }}
        {% endfor %}
    </div>
    <div class="col-lg-12 col-md-12 col-sm-1 bas">
        <div class="col-lg-4 col-md-4 col-sm-1 bas1">
            {% for content in zones.bas1 %}
                {{ content }}
            {% endfor %}
        </div>
        <div class="col-lg-4 col-md-4 col-sm-1 bas2">
            {% for content in zones.bas2 %}
                {{ content }}
            {% endfor %}
        </div>
        <div class="col-lg-4 col-md-4 col-sm-1 bas3">
            {% for content in zones.bas3 %}
                {{ content }}
            {% endfor %}
        </div>
    </div>
</div>