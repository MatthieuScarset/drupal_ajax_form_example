{#
/**
 * @file
 * Default theme implementation to display a paragraph.
 *
 * Available variables:
 * - paragraph: Full paragraph entity.
 *   Only method names starting with "get", "has", or "is" and a few common
 *   methods such as "id", "label", and "bundle" are available. For example:
 *   - paragraph.getCreatedTime() will return the paragraph creation timestamp.
 *   - paragraph.id(): The paragraph ID.
 *   - paragraph.bundle(): The type of the paragraph, for example, "image" or "text".
 *   - paragraph.getOwnerId(): The user ID of the paragraph author.
 *   See Drupal\paragraphs\Entity\Paragraph for a full list of public properties
 *   and methods for the paragraph object.
 * - content: All paragraph items. Use {{ content }} to print them all,
 *   or print a subset such as {{ content.field_example }}. Use
 *   {{ content|without('field_example') }} to temporarily suppress the printing
 *   of a given child element.
 * - attributes: HTML attributes for the containing element.
 *   The attributes.class element may contain one or more of the following
 *   classes:
 *   - paragraphs: The current template type (also known as a "theming hook").
 *   - paragraphs--type-[type]: The current paragraphs type. For example, if the paragraph is an
 *     "Image" it would result in "paragraphs--type--image". Note that the machine
 *     name will often be in a short form of the human readable label.
 *   - paragraphs--view-mode--[view_mode]: The View Mode of the paragraph; for example, a
 *     preview would result in: "paragraphs--view-mode--preview", and
 *     default: "paragraphs--view-mode--default".
 * - view_mode: View mode; for example, "preview" or "full".
 * - logged_in: Flag for authenticated user status. Will be true when the
 *   current user is a logged-in member.
 * - is_admin: Flag for admin user status. Will be true when the current user
 *   is an administrator.
 *
 * @see template_preprocess_paragraph()
 *
 * @ingroup themeable
 */
#}
{%
set classes = [
'paragraph',
'paragraph--type--' ~ paragraph.bundle|clean_class,
view_mode ? 'paragraph--view-mode--' ~ view_mode|clean_class,
]
%}
{% set lang = paragraph.get('langcode').getValue()[0].value %}

{% block paragraph %}
    <div{{ attributes.addClass(classes) }}>
        {% block content %}

            {# DESKTOP DISPLAY #}
            <div class="col-md-12 col-lg-12 hidden-sm hidden-xs home_banner_no_padding">
                <a class="home_banner_element_link" href="{{ content.field_link_title[0]['#url'] }}">
                    <div class="home_banner_next" id="smallimage" style="background:url( {{ file_url(content.field_little_image_desktop['#items'].entity.uri.value) }} )">
                    <!-- Bottom Container -->
                        <div class="home_banner_next_bottom_container col-md-12 col-lg-12 hidden-xs hidden-sm">

                                <!-- Left container / date-->
                                <div class="col-md-1 home_banner_next_bottom_left_container">
                                    <!-- Date -->
                                    <div class="home_banner_next_date">

                                        {% if (lang == 'fr') %}
                                            {% set date = content.field_date[0]['#attributes']['datetime'] | date('d') %}
                                            {{ date | trim }}<br>
                                            {{ content.field_date[0]['#attributes']['datetime'] | date('m') | trim }} <br>
                                            {{ content.field_date[0]['#attributes']['datetime'] | date('y') | trim }}
                                        {% else %}
                                            {% set date = content.field_date[0]['#attributes']['datetime'] | date('m') %}
                                            {{ date | trim }}<br>
                                            {{ content.field_date[0]['#attributes']['datetime'] | date('d') | trim }} <br>
                                            {{ content.field_date[0]['#attributes']['datetime'] | date('y') | trim }}
                                        {% endif %}
                                    </div>
                                </div>
                                <!-- Right container/ title -->
                                <div class="col-md-9 home_banner_next_bottom_right_container" style="color: {{ content.field_text_color[0]['#markup'] }};">
                                    <p class="home_banner_next_title" >
                                        {{ content.field_title[0]['#context']['value'] }}
                                    </p>
                                    <p class="home_banner_next_nameFunction">
                                        {% set name_function %}
                                            {{ content.field_name[0]['#context']['value'] }} | {{ content.field_function[0]['#context']['value'] }}
                                        {% endset %}
                                        {% if content.field_function[0]['#context']['value'] is not empty %}
                                            {{ name_function }}
                                        {% else %}
                                            {{ content.field_name[0]['#context']['value'] }}
                                        {% endif %}
                                    </p>


                                </div>
                            </div>

                    </div>
                </a>
            </div>

            {# MOBILE DISPLAY #}
            <div class="container no-padding">

                <div class="col-sm-12 col-xs-12 hidden-md hidden-lg">
                    <a class="home_banner_element_mobile_link" href="{{ content.field_link_title[0]['#url'] }}">

                         <div class="home_banner_mobile" style="background:url( {{ file_url(content.field_image_mobile['#items'].entity.uri.value) }} )">

                                <!-- Date -->
                                <div class="home_banner_next_date">

                                    {% if (lang == 'fr') %}
                                        {% set date = content.field_date[0]['#attributes']['datetime'] | date('d') %}
                                        {{ date | trim }}<br>
                                        {{ content.field_date[0]['#attributes']['datetime'] | date('m') | trim }} <br>
                                        {{ content.field_date[0]['#attributes']['datetime'] | date('y') | trim }}
                                    {% else %}
                                        {% set date = content.field_date[0]['#attributes']['datetime'] | date('m') %}
                                        {{ date | trim }}<br>
                                        {{ content.field_date[0]['#attributes']['datetime'] | date('d') | trim }} <br>
                                        {{ content.field_date[0]['#attributes']['datetime'] | date('y') | trim }}
                                    {% endif %}
                                </div>

                                <!-- Right container/ title -->
                                 <div class="home_banner_mobile_title" style="color: {{ content.field_text_color[0]['#markup'] }};">
                                    <p class="home_banner_title" >
                                        {{ content.field_title[0]['#context']['value'] }}
                                    </p>
                                 <p class="home_banner_first_nameFunction">
                                     {% set name_function %}
                                         {{ content.field_name[0]['#context']['value'] }} | {{ content.field_function[0]['#context']['value'] }}
                                     {% endset %}
                                     {% if content.field_function[0]['#context']['value'] is not empty %}
                                         {{ name_function }}
                                     {% else %}
                                         {{ content.field_name[0]['#context']['value'] }}
                                     {% endif %} </p>
                                </div>

                         </div>
                    </a>
                </div>

            </div>



        {% endblock %}
    </div>
{% endblock paragraph %}


