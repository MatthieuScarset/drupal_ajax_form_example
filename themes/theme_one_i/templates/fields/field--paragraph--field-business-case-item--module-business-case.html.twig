{% set col = items|length == 2 ? "col-md-6" : "col-md-4" %}

<div class="row business-case-items h-100">
    {% for item in items %}
      <div class="col-11 {{ col }} pb-1 pb-md-0 horizontal-align-item">{{ item.content }}</div>
    {% endfor %}
</div>

