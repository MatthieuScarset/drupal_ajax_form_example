{% set col = items|length == 2 ? "col-md-6" : "col-md-4" %}

<div class="row business-case-items">
    {% for item in items %}
      <div class="col-10 {{ col }} pb-1 pb-md-0">{{ item.content }}</div>
    {% endfor %}
</div>

