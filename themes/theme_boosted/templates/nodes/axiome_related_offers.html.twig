{% if (
      is_string(related_offers.offer1_url) and related_offers.offer1_url|trim is not empty
      and is_string(related_offers.offer1_custom_link) and related_offers.offer1_custom_link|trim is not empty)
   or (
      is_string(related_offers.offer2_url) and related_offers.offer2_url|trim is not empty
      and is_string(related_offers.offer2_custom_link) and related_offers.offer2_custom_link|trim is not empty)
   or (
      is_string(related_offers.offer3_url) and related_offers.offer3_url|trim is not empty
      and is_string(related_offers.offer3_custom_link) and related_offers.offer3_custom_link|trim is not empty)
%}
<div class="node-axiome-know-more col-md-12 nopadding">
    <div class="col col-md-12 nopadding">
        {% if is_string(related_offers.title) and related_offers.title|trim is not empty %}
            <h2 class="titre2">{{ related_offers.title }}</h2>
        {% endif %}
        <ul>
            {% if is_string(related_offers.offer1_url) and related_offers.offer1_url|trim is not empty
                and is_string(related_offers.offer1_custom_link) and related_offers.offer1_custom_link|trim is not empty %}
                <li><a href="{{ related_offers.offer1_url|url_clean_prefix }}" target="_blank">{{ related_offers.offer1_custom_link }}</a></li>
            {% endif %}
            {% if is_string(related_offers.offer2_url) and related_offers.offer2_url|trim is not empty
              and is_string(related_offers.offer2_custom_link) and related_offers.offer2_custom_link|trim is not empty %}
                <li><a href="{{ related_offers.offer2_url|url_clean_prefix }}" target="_blank">{{ related_offers.offer2_custom_link }}</a></li>
            {% endif %}
            {% if is_string(related_offers.offer3_url) and related_offers.offer3_url|trim is not empty
              and is_string(related_offers.offer3_custom_link) and related_offers.offer3_custom_link|trim is not empty %}
                <li><a href="{{ related_offers.offer3_url|url_clean_prefix }}" target="_blank">{{ related_offers.offer3_custom_link }}</a></li>
            {% endif %}
        </ul>
    </div>
</div>
{% endif %}
