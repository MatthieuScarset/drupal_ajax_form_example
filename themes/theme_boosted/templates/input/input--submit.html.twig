{#
/**
 * @file
 * Default theme implementation for an 'input' #type form element.
 *
 * Available variables:
 * - attributes: A list of HTML attributes for the input element.
 * - children: Optional additional rendered elements.
 * - icon: An icon.
 * - input_group: Flag to display as an input group.
 * - icon_position: Where an icon should be displayed.
 * - prefix: Markup to display before the input element.
 * - suffix: Markup to display after the input element.
 * - type: The type of input.
 *
 * @see \Drupal\bootstrap\Plugin\Preprocess\Input
 * @see template_preprocess_input()
 *
 * @ingroup templates
 */
#}
{% spaceless %}
  {% if input_group %}
    <div class="input-group">
  {% endif %}

  {% if prefix %}
    {{ prefix }}
  {% endif %}

  {% block input %}


    {% if attributes.hasClass('csp-list-map-search') %}
      {% if attributes.hasClass('search-icon') %}
        <button id="search-icon-btn" type="button" class="btn btn-default search-icon-btn">
          <span class="glyphicon glyphicon-search" aria-hidden="true"></span>
        </button>
        <input{{ attributes.addClass(["btn-ajax-hidden","btn","btn-default"]) }} />
        <button id="remove-icon-btn" type="button" class="btn btn-default remove-icon-btn hidden">
          <span class="glyphicon glyphicon-remove" aria-hidden="true"></span>
        </button>
      {% else %}
        <input{{ attributes.addClass(["btn","btn-default"]) }} />
      {% endif %}
    {% else %}
      <input{{ attributes.addClass(["btn","btn-default"]) }} />
    {% endif %}


  {% endblock %}

  {% if suffix %}
    {{ suffix }}
  {% endif %}

  {% if input_group %}
    </div>
  {% endif %}

  {{ children }}
{% endspaceless %}
